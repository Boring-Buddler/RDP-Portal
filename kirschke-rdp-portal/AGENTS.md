# Übergabe-Konvention

Nach jeder umgesetzten Änderung am Projekt eine einzelne, minimale Git-Commit-Nachricht anbieten: `v<Version>: <kurze Beschreibung>`.

Git-Commits selbst nur erstellen, wenn sie ausdrücklich angefordert werden.
